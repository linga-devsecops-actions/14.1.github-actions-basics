name: serial-dependency-jobs
on: [push] #trigger
jobs: #Job1 and Job4 are parellel. No dependency.
  job-1: #these shoud be unique
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: step-1
        run: echo "Hi, I am from job-1"

  job-2: #these shoud be unique
    needs: job-1
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: step-2
        run: echo "Hi, I am from job-2"

  job-3: #these shoud be unique
    needs: job-2
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: step-3
        run: echo "Hi, I am from job-3"

  job-4: #these shoud be unique
    needs: job-3
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: step-4
        run: echo "Hi, I am from job-4"

  job-5: #these shoud be unique
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: step-5
        run: echo "Hi, I am from job-5"
