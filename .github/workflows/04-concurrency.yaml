name: concurrency
on: [push] #trigger
concurrency:  # this workflow should be triggered for any branch only once.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true #Existing will be cancelled and latest will run. 
  
jobs:
  hello:
    runs-on: ubuntu-latest # Linux Os. We can have windows or MAC
    steps: # stages
      - name: Testing the concurrency
        run: sleep 20